class SupabaseConfig {
  // TODO: Replace these with your actual Supabase project credentials
  // You can find these in your Supabase project settings at https://app.supabase.io

  static const String supabaseUrl = 'YOUR_SUPABASE_URL';
  static const String supabaseAnonKey = 'YOUR_SUPABASE_ANON_KEY';

  // Instructions to set up:
  // 1. Go to https://app.supabase.io
  // 2. Create a new project or select existing one
  // 3. Go to Project Settings > API
  // 4. Copy the URL and paste it above
  // 5. Copy the anon/public key and paste it above
  // 6. Enable Email Auth in Authentication > Providers
  // 7. (Optional) Configure Google and Apple OAuth providers
}
